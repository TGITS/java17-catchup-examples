package tgits.random;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAlgorithmResourceIT extends AlgorithmResourceTest {

    // Execute the same tests but in native mode.
}